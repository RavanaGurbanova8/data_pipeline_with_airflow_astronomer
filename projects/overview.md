# Projects

1. Data Pipelines with Apache Airflow(airflow-book) - codes of all chapters of book. There are 17 chapters given in this project and each chapter has code examples. It helps to learn and practise all sections of every chapter.
2. Composing up data model in Docker. I 've composed up data model without loading data to Docker. Instead of it I 've mounted data to volume so it manages the access to the data. 
3. Minio task. I've created buckets in Minio, then uploaded parquet files based on given date.
4. 